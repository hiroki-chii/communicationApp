# 社内ランチ交流会マッチングシステム 運用マニュアル

本システムは、Google Apps Script (GAS) をバックエンドとし、スプレッドシートをデータベースとして活用する、社内コミュニケーション活性化のためのWebアプリケーションです。Gemini AIによる趣味・価値観を考慮したインテリジェントなグループマッチング機能、メンバー詳細ポップオーバー、リアルタイムグループチャット機能、およびセキュアな管理者向け管理機能を搭載しています。

---

## 1. システム概要とアーキテクチャ

### 1.1 全体アーキテクチャ
*   **フロントエンド**: シングルページアプリケーション (SPA) 構成、レスポンシブデザイン（PC・モバイル両対応）、アースカラー（オレンジ＆グリーン）のブランドデザイン、ライト/ダークモード対応。
*   **バックエンド**: Google Apps Script (main.gs)
*   **データベース**: Google スプレッドシート（「メンバー一覧」「設定」「部署マスタ」「マッチング履歴」などのシート）
*   **マッチングエンジン**: Gemini APIによるセマンティックマッチング ＆ 独自高速山登り法アルゴリズムのハイブリッド構成。

---

## 2. ユーザー権限とアクセス制御

### 2.1 権限の種類
システムには「一般ユーザー」と「管理者」の2つの権限が存在します。ログイン情報には、アクセスしたGoogleアカウントのメールアドレス (`Session.getActiveUser().getEmail()`) が自動的に使用されます。

*   **一般ユーザー**:
    *   システムへログインした全社員が該当します。
    *   次回マッチング結果の閲覧、「自分を探す」検索機能、グループチャット、自身のプロフィールの自己登録・更新が可能です。
*   **管理者**:
    *   スプレッドシートの「設定」シート内の `admin_emails` に登録されているユーザーです。
    *   システム初期化時に、最初に実行したユーザー（ヒロキ）が自動的に初代管理者に設定されます。
    *   一般ユーザー機能に加え、管理者ダッシュボード、AIマッチング生成・微調整、全メンバー管理（代理編集）、システム設定（APIキー、管理者追加）へアクセスできます。

---

## 3. 一般ユーザー向け機能と操作方法

### 3.1 次回マッチング確認（デフォルト画面）
ログイン後、最初に表示されるメイン画面です。最新の確定したマッチング結果（グループ分け）がカード形式で美しく表示されます。
1.  **グループ情報の閲覧**:
    *   各グループカードには、Gemini AIがメンバーの趣味や価値観を分析して命名した「ユニークなグループ名」と、会話を盛り上げるための「おすすめ雑談テーマ」が表示されます。
2.  **メンバー詳細ポップオーバー**:
    *   メンバーの名前にホバー（PC）またはタップ（スマホ）すると、スリムなポップオーバーが表示され、所属部署や「趣味」を確認できます。
3.  **「自分を探す」検索機能**:
    *   画面上部の検索窓に自分の名前やメールアドレスを入力すると、自分が所属しているグループカードがオレンジとグリーンのブランドグラデーションで強調表示（フォーカス）され、即座に自分のグループを見つけることができます。

### 3.2 プロフィール登録・変更
社員が自身のプロフィール情報を登録・更新する画面です。
*   **情報の自動読み込み**:
    *   自身のメールアドレスを入力すると、既に登録されているプロフィール情報（名前、部署、趣味、価値観等）が自動的にフォームにロードされます。
*   **入力項目**:
    *   **名前** / **所属部署**（部署リストから選択。「その他」を選択した場合は自由記述が可能）
    *   **参加形式**: 「対面」「リモート」「どちらでも」から選択できます。
    *   **趣味 / 特技 / 弱点**: それぞれ**全角25文字以内**で入力します。リアルタイム文字数カウンターが文字数を表示し、上限に近づくと警告色（黄→赤）に変化します。
    *   **仕事・人生の価値観（5段階評価）**: 「やりがい」「チームワーク」「プライベート」「評価」「成長」の5項目について、自身が重視する度合いを1（低）〜5（高）で選択します。
    *   **ステータス（アクティブ / 非アクティブ）**: 次回ランチ会に参加可能かどうかを切り替えます。非アクティブに設定すると、マッチング対象から除外されます。
    *   **次回優先フラグ**: 次回ランチ会に「優先的に参加したい」場合にチェックを入れます。
    *   **カード用画像（プロフィール写真）**:
        *   ランチ会の実物カード印刷などに使用する写真をアップロードできます（ファイルサイズは最大5MBまで、画像ファイルのみ）。
        *   写真を選択または削除しようとすると、画面中央に確認のメッセージ（ポップアップ）が表示されます。
        *   **重要**: 写真の追加や削除などの変更は、確認メッセージで「設定する」や「削除する」を選んだだけでは確定されません。フォームの最下部にある**「変更を保存する」ボタンを押した時点で初めて保存・確定**されます。

### 3.3 グループチャット機能
マッチング確定後、同じグループのメンバー同士でリアルタイムに連絡を取り合えるチャットスペースです。
*   対面ランチの場所決め、リモートランチのURL共有、日程調整に利用できます。
*   チャットヘッダー内のメンバーアイコン一覧から、各メンバーの「趣味」などをポップオーバーで手軽に確認できます。

---

## 4. 管理者向け機能と操作方法

管理者は、サイドバーまたはボトムナビゲーションから「管理者パネル」に切り替えることができます。

### 4.1 管理者ダッシュボード
*   現在の「登録メンバー総数」「次回参加予定のアクティブユーザー数」「過去のマッチング実施回数」などのサマリー数値を確認できます。

### 4.2 インテリジェントAIマッチングの生成
新規のマッチンググループを作成・確定する手順です。
1.  **パラメータの設定**:
    *   1グループあたりの目標人数（標準は4人）を指定します。
2.  **マッチング生成の実行**:
    *   「Gemini AIマッチング」または「山登り法（高速ロジック）」を選択してマッチングを生成します。
    *   Gemini AIマッチングでは、メンバーが登録した「趣味」「特技」「弱点」「仕事・人生の価値観（5段階スコア）」および「参加形式（対面/リモート）」の親和性を総合的に分析し、会話が最も弾む組み合わせを全自動で編み出します。
3.  **手動ドラッグ＆ドロップ調整**:
    *   生成された仮グループのメンバーを、管理者自身がマウス操作でドラッグ＆ドロップして他のグループへ移動させ、最終微調整を行うことができます。
4.  **マッチングの確定・公開**:
    *   「マッチングを確定する」ボタンを押すと、結果が「マッチング履歴」シートに保存され、一般ユーザー画面に最新結果として公開されます。

### 4.2.1 Geminiが参照・考慮するユーザー情報
Gemini AIによるマッチング実行時、バックエンドからGemini APIに対して、以下のユーザー情報がオブジェクト形式でセキュアに連携され、グループ選出および雑談テーマの考案に活用されます。

1.  **基本属性情報**:
    *   **メンバーID / 名前 / 所属部署 (dept)**: 部署の重複を避けて「部署の多様性」を最大化するために活用されます。
2.  **パーソナル属性 (自己紹介項目)**:
    *   **趣味 (interests)**: 共通の興味関心を持つメンバーを繋げ、会話の強力なフックを作ります。
    *   **特技 (specialty)**: メンバー個々の強みやユニークなスキルから、盛り上がるトピックを引き出します。
    *   **弱点 (weakness)**: 人間味のある自己開示による親近感の醸成、またはお互いにクスッと笑い合えるような雑談テーマの生成に活用されます。
3.  **仕事・人生の価値観 (values - 5段階スコア)**:
    *   「やりがい」「チームワーク」「プライベート」「評価」「成長」の5つについて、各スコアの親和性や価値観のバランスを多角的に分析し、居心地が良く刺激的な組み合わせを導き出します。
4.  **配慮事項 (considerations) の尊重と非開示制御**:
    *   アレルギーや時間制限などの配慮事項が登録されている場合、Geminiはそれらの要望を尊重したグループ分けを行います。
    *   **セキュリティとプライバシー保護**: デリケートな情報（アレルギーや健康状態など）の漏洩を防ぐため、Geminiが自動作成する全体公開用の紹介文（`memo`）には、**「配慮事項」に関する具体的な内容は一切出力しないようにシステムインストラクションによって厳格に制御**されています。

### 4.3 メンバー管理（マスタメンテナンス）
登録されている全メンバーの一覧をテーブル形式で管理します。
*   **価値観のカラーバッジ可視化**:
    *   メンバーの価値観5項目のスコアが、5（エメラルド）、4（スカイブルー）、3（スレートグレー）、2（アンバー）、1（ローズ）の濃いソリッドカラーバッジで表示され、視覚的に傾向を把握できます。
*   **参加ステータス・次回優先の即時トグル**:
    *   テーブル上のチェックボックスをクリックするだけで、サーバーサイドのスプレッドシート値が瞬時に更新され、日本語でわかりやすい完了トーストが表示されます。
*   **代理登録・編集モーダル**:
    *   管理者は、メンバーの「新規追加」や「登録情報の編集」を代理で行うことができます。一般画面と同様の文字数バリデーションや部署選択ロジック（「その他」対応）が完全に適用されます。
*   **一括削除・検索フィルター**:
    *   部署（「その他」含む）や名前、メールアドレスによる瞬時のテーブル内検索フィルタリングが可能です。

### 4.4 システム設定
*   **管理者リストの追加・編集**: カンマ区切りで管理者メールアドレスを編集・更新できます。
*   **Gemini APIキーの設定**: Gemini APIの接続検証テストを行えます。
*   **データベース初期化（リセット）**: 初期状態に戻したり、テスト用デモメンバーを一括で自動生成することができます。

### 4.5 プロフィール画像の保管とGoogleドライブ連携仕様（管理者向け）
本システムにアップロードされたプロフィール画像は、Googleドライブ上で安全かつ自動的に管理されます。
1.  **画像の保存場所**:
    *   本システムで使用しているスプレッドシートと同じフォルダ内に、**「プロフィール画像」という名前のフォルダが自動的に作成**されます。アップロードされた画像はこのフォルダ内に自動保存されます。
2.  **アクセスの自動管理（誰でも閲覧可能な設定）**:
    *   保存された写真は、メンバー同士がマッチング画面やチャット画面で互いに見られるように、**「リンクを知っている全員が閲覧可能」という共有設定が自動で適用**されます。
    *   *注意*: 「プロフィール画像」フォルダ全体のアクセス権ではなく、アップロードされた各写真ファイルに対して個別に共有設定が行われるため、セキュリティ上安全です。
3.  **自動整理による容量の節約**:
    *   ユーザーが写真を変更したり削除したりすると、**古い写真は自動的にGoogleドライブの「ゴミ箱」へ移動**されます。これにより、使わなくなった画像ファイルがサーバーに残り続けて容量を圧迫することを防ぎます。
4.  **スプレッドシートへの記録**:
    *   「メンバー一覧」スプレッドシートの「プロフィール画像」列には、その写真を表示するためのGoogleドライブ上の専用URL（例: `https://lh3.googleusercontent.com/d/画像ファイルID`）が保存されます。

---

## 5. 運用上の注意点 ＆ トラブルシューティング

### 5.1 スプレッドシートの直接編集について
*   **注意**: スプレッドシートのヘッダー名（1行目）や列順を、手動で直接変更しないでください。プログラムの読み書き位置（インデックス）とズレが生じ、システムエラーの原因となります。
*   **カラム構成の自動修復機能**:
    *   本システムには自動修復機能が実装されています。万が一、「参加形式」や「プロフィール画像」などの新しく追加された列が存在しない場合、アプリ起動時に安全に現在のデータを一時保存（退避）した上で、正しい17列の構成に自動で作り直し、不整合なデータを自動的にきれいに整理（クレンジング）します。

### 5.2 アプリの更新・デプロイ方法（管理者向け）
プログラムの修正を反映させる手順です。
1.  **claspを用いたプッシュ**:
    *   ファイルの修正後、以下のコマンドを使用してGoogle Apps Scriptへコードをプッシュします。
    ```bash
    clasp push
    ```
2.  **Web Appのデプロイバージョンの更新**:
    *   スプレッドシートのメニューから `拡張機能 -> Apps Script` を開きます。
    *   右上にある `デプロイ -> デプロイの管理` をクリックします。
    *   有効なWeb Appデプロイの編集（鉛筆マーク）をクリックし、バージョンを「新バージョン」として保存します。
    *   **重要**: アクセスできるユーザーが「全員（匿名を含む、または組織内）」、実行ユーザーが「アクセスしているユーザー」または「自分（開発者）」の要件に合わせて適切に選択されていることを確認してください。

### 5.3 よくある質問 (FAQ)
*   **Q. 趣味・特技・弱点が25文字を超えて登録できないのはなぜですか？**
    *   A. ポップオーバーやグループカードなどの限られたUI領域で文字がはみ出さず、美しくコンパクトに収めるための仕様です。

---

## 6. メンテナンスマニュアル (開発者・保守担当者向け)

本セクションは、システムの仕様変更、データベースの拡張、バグ調査を行う開発者および保守担当者向けのマニュアルです。

### 6.1 スプレッドシート（データベース）スキーマ詳細
システムが読み書きする各スプレッドシートの物理的なスキーマ構成です。

#### ①「メンバー一覧」シート（全17列）
| 列番号 | カラム名 | データ型 | 説明 |
| :--- | :--- | :--- | :--- |
| A (1) | `メンバーID` | 文字列 | `M001`, `M002` ... 形式の自動採番ユニークID |
| B (2) | `名前` | 文字列 | メンバーの氏名 |
| C (3) | `メールアドレス` | 文字列 | Googleアカウントのメールアドレス（小文字統一） |
| D (4) | `部署・チーム` | 文字列 | 所属部署名（部署マスタの値、または「その他」による自由入力値） |
| E (5) | `参加形式` | 文字列 | `対面` / `リモート` / `どちらでも` のいずれか |
| F (6) | `趣味` | 文字列 | 趣味（最大全角25文字） |
| G (7) | `特技` | 文字列 | 特技（最大全角25文字） |
| H (8) | `弱点` | 文字列 | 弱点（最大全角25文字） |
| I (9) | `やりがい` | 数値 | 仕事・人生の価値観（1〜5） |
| J (10)| `チームワーク` | 数値 | 仕事・人生の価値観（1〜5） |
| K (11)| `プライベート` | 数値 | 仕事・人生の価値観（1〜5） |
| L (12)| `評価` | 数値 | 仕事・人生の価値観（1〜5） |
| M (13)| `成長` | 数値 | 仕事・人生の価値観（1〜5） |
| N (14)| `配慮事項` | 文字列 | アレルギーや時間制限などの個別要望（全体非公開） |
| O (15)| `ステータス` | 真偽値 | 参加状態（チェックボックス: `TRUE` = アクティブ、`FALSE` = 非アクティブ） |
| P (16)| `次回優先` | 真偽値 | 次回優先フラグ（チェックボックス: `TRUE` = 優先選出、`FALSE` = 通常） |
| Q (17)| `プロフィール画像` | 文字列 | プロフィール画像のGoogleドライブ保存URL |

#### ②「設定」シート（全3列）
*   **A列**: `設定キー` （`admin_emails`, `gemini_api_key`, `webapp_url`）
*   **B列**: `設定値`
*   **C列**: `説明`

#### ③「部署マスタ」シート（全2列）
*   **A列**: `部署ID` （`D001`, `D002` ...）
*   **B列**: `部署名`

#### ④「マッチング履歴」シート（全6列）
*   **A列**: `開催日` （`YYYY-MM-DD`）
*   **B列**: `グループID` （`G-1`, `G-2` ...）
*   **C列**: `メンバーID一覧` （カンマ区切り、例: `M001,M003`）
*   **D列**: `メンバー名一覧` （カンマ区切り、例: `山田 太郎, 鈴木 健一`）
*   **E列**: `マッチング方法` （`gemini`, `logic`, `logic_fallback` など）
*   **F列**: `選出理由 / メモ` （Geminiが考案した雑談テーマおよびグループ説明文）

#### ⑤「チャットメッセージ」シート（全8列）
*   **A列**: `メッセージID` （`MSG001` ...）
*   **B列**: `ルームID` （`YYYY-MM-DD_グループID`）
*   **C列**: `送信者ID`
*   **D列**: `送信者名`
*   **E列**: `送信者メール`
*   **F列**: `部署名`
*   **G列**: `メッセージ内容`
*   **H列**: `送信日時` （ISO 8601形式文字列）

---

### 6.2 データベース自動同期とマイグレーションの仕組み (`initDatabase`)
アプリケーション起動時 (Web Appへのアクセス、またはスプレッドシート上のメニューからの手動初期化実行時) に `initDatabase()` がコールされ、以下の修復マイグレーションが自動実行されます。
1.  **不足列の自動検知と挿入**:
    *   「メンバー一覧」シートのヘッダー数が17列未満である、または列順のズレ（例: 旧仕様の「参加目的」が残っている等）を検知すると、既存の全データをメモリ上に一度完全に退避します。
    *   その後、正しい17列順にマッピングし直し、シート全体をクリーンアップした上で再配置し上書き修復します。
2.  **型異常データ検証ルールのクレンジング**:
    *   K〜O列などに誤って埋め込まれたチェックボックスを完全に初期化（`clearDataValidations()`）し、チェックボックスを正しい **15列目 (ステータス)** および **16列目 (次回優先)** にのみ正しくバインドし直します。

---

### 6.3 主要ロジックと最適化設計

#### ① 山登り法（ローカルサーチ）高速化アルゴリズム (`runLogicMatching`)
GASの実行時間制限（最大6分）によるタイムアウトを回避するため、極めて高度な最適化が施されています。
*   **ボトルネックの解消**: ループ内での `members.find()` 等の線形探索（$O(N)$）を完全に廃止しました。
*   **Map構造の採用**: 関数開始時にメンバーデータを JavaScript の `Map` オブジェクトへ一括変換（$O(N)$ で構築）し、イテレーション内では **`Map.get(id)`** を使用して **$O(1)$** で高速アクセスします。
*   **ペナルティ定義**:
    *   同グループ内の「過去の重複履歴」に対して：1重複あたり `+100` ペナルティ
    *   同グループ内の「部署の被り」に対して：1ペアあたり `+40` ペナルティ
    *   同グループ内の「共通の趣味キーワード（サウナ、カフェ等）」に対して：1ペアあたり `-10` ボーナス
    *   グループサイズと目標人数の乖離に対して：`差分 * 20` ペナルティ
*   **試行回数**: 2,500回のスワップイテレーションを実施し、スコアが最も低く最適化されたグループ構成をミリ秒単位で高速算出します。

#### ② Gemini AI マッチング (`runGeminiMatching`)
*   **通信仕様**: 接続には `gemini-3.5-flash` モデルを使用し、低コストかつ極めて高速にレスポンスを得られます。
*   **入力データ**: `id`, `name`, `dept`, `interests`, `specialty`, `weakness`, `values` (Motivation等5項目), `considerations` をオブジェクトとしてAPIへ送信します。
*   **出力保証**: 構造化されたJSON（`responseMimeType: "application/json"`）で直接受け取ることで、パースエラーを完全に防止しています。

---

### 6.4 データベース拡張時のチェックリスト（開発手順）
将来的に新しいプロフィール項目を追加するなど、データベースを拡張する際の実装変更箇所です。

1.  **`main.gs` / `initDatabase()` の修正**:
    *   `targetHeaders` 配列の末尾に新しい列ヘッダー名を追加する。
    *   NeedsMigration判定および、退避データから新配列へマッピングする際のデフォルト値フォールバック処理を追記する。
    *   チェックボックスのインデックス（`15`, `16`列目）がずれる場合は、バインド対象列の数値を更新する。
2.  **`main.gs` / `buildMemberRow()` の修正**:
    *   受け取ったオブジェクトからスプレッドシートの1行配列（例: 18列）へマッピングする処理に、新しいプロパティを追加する。
3.  **`main.gs` / `getMembers()` の修正**:
    *   `sheet.getRange` の取得列数を拡張する。
    *   `data.map` で返却するメンバーオブジェクトに、新カラムの値をプロパティ（例: `row[17]`）として割り当てる。
4.  **`main.gs` / `registerSelfProfile()` ＆ `addMemberToSheet()` ＆ `updateMemberInSheet()` の修正**:
    *   フロントエンドから送信されたパラメータをオブジェクトにマッピングして登録・更新する処理へ新プロパティを追記する。
5.  **`index.html` (フロントエンド) の修正**:
    *   プロフィール登録画面（`getUserProfileViewHtml`）に入力用UI要素を追加。
    *   自己プロフィール保存（`handleSaveSelfProfile`）および管理者側メンバー保存（`handleSubmitMember`）でのDOM値取得およびバリデーション処理を追加。
    *   管理者側メンバー一覧テーブルおよび代理登録モーダルUIに表示・編集欄を追加。

---

### 6.5 デバッグ・ログ監視
*   システム内の主要な例外はすべて `Logger.log()` に出力されます。
*   GASエディタの「実行数」タブ、またはGoogle Cloud Platform (GCP) の「Logs Viewer」を連携させることで、エラーの詳細なスタックトレースを容易に追跡可能です。

